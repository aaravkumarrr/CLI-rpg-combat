still need to implement dodging mechanics
fireball mechanics need to be reworked to reset everytime a game loop so it can be used against a diff enemy


Stretch:
Inventory system
Add an Item class. Let the player carry potions or weapons that modify stats during combat.

Dungeon mode
Fight a sequence of enemies back to back. Each win restores a little health before the next fight.

Save system
Persist the player's name, class, and win count to a JSON file between sessions.

Rich CLI
Use the rich library to render a health bar and colour-coded combat log each round.
